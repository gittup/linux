include_rules
#
# Tupfile for the linux journaling routines.
#

obj-$(CONFIG_JBD) += jbd.o

jbd-objs := transaction.c commit.c recovery.c checkpoint.c revoke.c journal.c
include $(LINUX_ROOT)/linux2.tup
