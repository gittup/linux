include_rules
#
# Tupfile for the linux ramfs routines.
#

obj-y += ramfs.o

file-mmu-y := file-nommu.c
file-mmu-$(CONFIG_MMU) := file-mmu.c
ramfs-objs += inode.c $(file-mmu-y)
include $(LINUX_ROOT)/linux2.tup
