include_rules
modname = intelfb

intelfb-y := intelfbdrv.c intelfbhw.c
intelfb-@(FB_INTEL_I2C) += intelfb_i2c.c
obj-@(FB_INTEL) += $(intelfb-y)

ifdef FB_INTEL_DEBUG
#EXTRA_CFLAGS += -DDEBUG -DVERBOSE -DREGDUMP
EXTRA_CFLAGS += -DDEBUG -DREGDUMP
endif
include $(LINUX_ROOT)/linux.tup
