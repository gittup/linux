include_rules
modname = algos
#
# Makefile for the i2c algorithms
#

obj-@(I2C_ALGOBIT)	+= i2c-algo-bit.c
obj-@(I2C_ALGOPCF)	+= i2c-algo-pcf.c
obj-@(I2C_ALGOPCA)	+= i2c-algo-pca.c

ifeq (@(I2C_DEBUG_ALGO),y)
EXTRA_CFLAGS += -DDEBUG
endif
include $(LINUX_ROOT)/linux.tup
